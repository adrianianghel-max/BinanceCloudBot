from __future__ import annotationsimport loggingimport timefrom typing import Anyimport ccxtimport configfrom indicators import (    add_ema_columns,    calculate_ema10_slope_pct,    calculate_growth_score,    calculate_macd_values,    calculate_rsi_pair,    calculate_volume_ratio,    is_daily_bullish,    prepare_ohlcv_df,)from state_manager import (    get_alert_state,    should_send_only_new,    update_alert_state,)from telegram_sender import send_telegram_messagelogging.basicConfig(    level=logging.INFO,    format="%(asctime)s | %(levelname)s | %(name)s | %(message)s",)logger = logging.getLogger("binance_usdc_scanner")def with_retries(func, *args, **kwargs):    delay = config.INITIAL_RETRY_DELAY
